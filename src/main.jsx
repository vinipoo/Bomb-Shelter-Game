import React from "react"
import ReactDOM from "react-dom/client"
import ShelterBet from "./App.jsx"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ShelterBet />
  </React.StrictMode>
)
